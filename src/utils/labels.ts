
export enum headers {
  cities = "Cities",
  expreiences = "Experiences",
  explorer = "Explorer",
};

export enum labels {
  shareThisContent = "Share this content on:"
};

export enum buttons {
  readMore = "Read More",
}